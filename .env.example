# Kill switch: set to "true" to disable this campaign page and show a
# "campaign not found" wall redirecting visitors to our main sites.
# Vite env is build-time, so flipping this requires a redeploy.
VITE_CAMPAIGN_DISABLED=false
